[project]
name = "quarry"
version = "0.2.0"
description = "PubMed-primary academic paper exploration + citation graph mining system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb",
    "lancedb",
    "numpy",
    "orjson",
    "blake3",
    "pyarrow",
    "pydantic-settings",
    "httpx",
    "lxml",
    "rustworkx",
    "sentence-transformers",
    "torch",
    "mcp[cli]",
]
